# Regenesys Private GPT — Frontend

A React + Vite frontend for the Regenesys PrivateGPT enterprise AI assistant.

## Getting Started

```bash
cd frontend
npm install
npm run dev
```

The app will be available at `http://localhost:5173`.

## Build for Production

```bash
cd frontend
npm run build
```
